# Frontend (Vite) — used at BUILD time only, not uploaded to server
# Local dev:
VITE_API_URL=http://localhost:3001
# Production build (before npm run build):
# VITE_API_URL=https://api.danielasantos.work
